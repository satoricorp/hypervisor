# Next: M4 — worktrees

**M8b is the last remote milestone; next build target is M4 (worktrees) —
planner writes the task file.**

M8b shipped 2026-07-11 (iMessage bridge — self-chat grammar, approvals gated
off by default). Live phone DoD blocked on Full Disk Access; evidence in
`tasks/M8b.md`.
