# Staging key for local `npm run tauri dev` (hypervisor-staging project).
# Production key lives in GitHub Actions secrets for release builds.
POSTHOG_PROJECT_KEY=phc_…
POSTHOG_HOST=https://us.i.posthog.com
